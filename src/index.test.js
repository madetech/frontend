import { Footer, Header, Pagination, Prose, TopBar } from './index'

it('exports components', () => {
  expect(Footer).toBeTruthy()
  expect(Header).toBeTruthy()
  expect(Pagination).toBeTruthy()
  expect(Prose).toBeTruthy()
  expect(TopBar).toBeTruthy()
})
